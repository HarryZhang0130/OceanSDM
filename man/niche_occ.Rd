% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/niche_occ.R
\name{niche_occ}
\alias{niche_occ}
\title{Estimate preference and tolerance ranges for a species based on occurrence data}
\usage{
niche_occ(
  occ_path,
  env_path,
  toler_quantiles = c(0.01, 0.99),
  pref_method = c("FWHM", "quantile"),
  pref_quantiles = c(0.25, 0.75),
  save_path = NULL,
  min_year,
  var_name = "thetao_mean",
  env_label = var_name,
  unit = "",
  lon_range,
  lat_range,
  width = 6,
  height = 5
)
}
\arguments{
\item{occ_path}{character. Path to occurrence file (table with columns: long, lat, year, month).}

\item{env_path}{character. Path to NetCDF file containing environmental data (e.g., temperature, DO).}

\item{toler_quantiles}{vector. Quantiles for tolerance range (default: c(0.01, 0.99)).}

\item{pref_method}{character. Method for preference range: "FWHM" (Full Width at Half Maximum) or "quantile" (default: "FWHM").}

\item{pref_quantiles}{vector. Quantiles for preference range when pref_method = "quantile" (default: c(0.25, 0.75)).}

\item{save_path}{character. Directory to save output plot and summary CSV. If NULL, nothing saved.}

\item{min_year}{numeric. First year of occurrence data (used to compute time index in NetCDF).}

\item{var_name}{character. Variable name in the NetCDF file (e.g., "thetao_mean", "o2").}

\item{env_label}{character. Display name for the environmental variable (e.g., "Temperature", "Dissolved Oxygen"). Defaults to var_name.}

\item{unit}{character. Unit of the environmental variable (e.g., "°C", "mg/L"). Defaults to empty string.}

\item{lon_range}{numeric vector of length 2: min and max longitude of the raster extent.}

\item{lat_range}{numeric vector of length 2: min and max latitude of the raster extent.}

\item{width}{numeric. Plot width in inches (default: 6).}

\item{height}{numeric. Plot height in inches (default: 5).}
}
\value{
A list containing:
\item{occ_values}{Extracted environmental values at occurrence points (NAs removed)}
\item{summary}{Data frame with optimal value, preference range, tolerance range}
\item{density_df}{Kernel density estimation results}
\item{pref_method}{Method used for preference range}
\item{pref_quantiles}{Preference quantiles (if applicable)}
\item{env_label}{Display name of the variable}
\item{unit}{Unit of the variable}
}
\description{
Extracts environmental values (e.g., temperature, dissolved oxygen) from raster layers
at occurrence points, then estimates optimal value, preference range, and tolerance range.
}
\examples{
\donttest{
niche_occ(
  occ_path = "F:/whaleshark_sdm/occ/R_typus_q1.txt",
  env_path = "F:/whaleshark_sdm/cmems/IndoWPac_temp_1m_y2015_y2024.nc",
  toler_quantiles = c(0.01, 0.99),
  pref_method = "FWHM",
  pref_quantiles =NULL,
  save_path = "F:/whaleshark_sdm/temp_fit/habitat"
  min_year = 2015,
  var_name = "thetao_mean",
  env_label = "Temperature",
  unit = "°C",
  lon_range = c(90, 180),
  lat_range = c(-41, 40)
)
}
}
