% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TB_varimp.r
\name{TB_varimp}
\alias{TB_varimp}
\title{Plot variable importance boxplot (applicable for any number of time periods)}
\usage{
TB_varimp(
  model_list,
  time_names = NULL,
  colors = NULL,
  title = NULL,
  x_label = "Importance",
  y_label = NULL,
  width = 5,
  height = 6,
  save_path = NULL
)
}
\arguments{
\item{model_list}{Named list. Contains model results for each time period,
e.g., \code{list(Quarter1 = sdm_q1, Quarter2 = sdm_q2, ...)}. Each element must
contain a data frame \code{var_imp} with columns \code{Var} (variable name) and \code{Im} (importance).}

\item{time_names}{Character vector. Names of time periods, e.g.,
\code{c("1st Quarter", "2nd Quarter", "3rd Quarter", "4th Quarter")}. Default is
\code{paste0("Time ", 1:n_times)}.}

\item{colors}{Optional character vector of colors, length equal to number of
time periods. Defaults to \code{RColorBrewer::brewer.pal(3, "Set3")} (or \code{rainbow}
if more than 12).}

\item{title}{Character. Plot title. Default is \code{NULL}.}

\item{x_label}{Character. x‑axis label. Default: \code{"Importance"}.}

\item{y_label}{Character. y‑axis label. Default: \code{NULL} (no label).}

\item{width}{Numeric. Plot width in inches for saving. Default: \code{5}.}

\item{height}{Numeric. Plot height in inches for saving. Default: \code{6}.}

\item{save_path}{Character. Path to save the plot (TIFF format). If \code{NULL},
the plot is not saved.}
}
\value{
A \code{ggplot} object of the variable importance boxplot.
}
\description{
Plot variable importance boxplot (applicable for any number of time periods)
}
\examples{
\donttest{
# Prepare model list (four quarters) containing model object from TB_sdm()
model_list <- list(
  Q1 = sdm_q1,
  Q2 = sdm_q2,
  Q3 = sdm_q3,
  Q4 = sdm_q4
)

# Time period names
time_names <- c("1st Quarter", "2nd Quarter", "3rd Quarter", "4th Quarter")

# Plot variable importance
imp_plot <- TB_varimp(model_list,
                      time_names = time_names,
                      save_path = "./variable_importance.tif")
}
}
